---
description: Show cubby status — active namespace and available secret names
---

Run `cubby ns` to show the active namespace and how it was resolved, then run
`cubby list` to show the secret names available in it. Do not reveal any values.
